current_env = "prod"

dev_config = {
    "env": "dev",
}

prod_config = {
    "env": "prod",
    "aws_access_key_id": "",
    "aws_secret_access_key": ""
}

config = {
    "prod": prod_config,
    "dev":dev_config
}
